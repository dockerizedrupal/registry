version: 0.1
log:
  level: debug
storage:
  cache:
    layerinfo: inmemory
  filesystem:
    rootdirectory: /registry/data/v2
  maintenance:
    uploadpurging:
      enabled: false
http:
  addr: :5000
  secret: asecretforlocaldevelopment

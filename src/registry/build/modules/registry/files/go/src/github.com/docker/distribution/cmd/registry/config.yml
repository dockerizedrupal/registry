version: 0.1
log:
  level: debug
storage:
  cache:
    layerinfo: inmemory
  filesystem:
    rootdirectory: /registry/data
  maintenance:
    uploadpurging:
      enabled: false
http:
  addr: :5000
  secret: asecretforlocaldevelopment

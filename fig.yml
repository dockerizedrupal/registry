registry:
  image: simpledrupalcloud/registry:latest
  hostname: registry
  ports:
    - "80:80"
  volumes_from:
    - registrydata
registrydata:
  image: simpledrupalcloud/data:latest
  hostname: registrydata
  volumes:
    - /registry/data

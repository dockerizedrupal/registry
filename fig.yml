registry:
  image: simpledrupalcloud/registry:dev
  hostname: registry
  ports:
    - "443:443"
  volumes_from:
    - registrydata
registrydata:
  image: simpledrupalcloud/data:dev
  hostname: registrydata
  volumes:
    - /registry/data
